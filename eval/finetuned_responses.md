# Finetuned Model Responses (raw)

Copy the verbatim raw outputs from the fine-tuned model here — one response per held-out evaluation document (20 documents). Each response must be prefixed by the filename or id of the test document.

Example format:

```
document_001.txt
<raw fine-tuned model response copied verbatim>
```

(Placeholders — populate after running the fine-tuned model in LlamaFactory.)
