# Baseline Responses (raw)

Copy the verbatim raw outputs from the base Llama 3.2 model here — one response per held-out evaluation document (20 documents). Each response must be prefixed by the filename or id of the test document.

Example format:

```
document_001.txt
<raw model response copied verbatim>
```

(Placeholders — populate after running the model in LlamaFactory or WebUI.)
